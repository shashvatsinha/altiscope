# Milestone 1 release candidate: One change, explained

Status: implementation under review; **not released**. Parent [#4](https://github.com/shashvatsinha/altiscope/issues/4).
No main merge or release tag is authorized by component checks alone.

## Changes

- Claims-only v2 output and explicit citation-valid/unverified publication contract (#11).
- Tight quotes, unique hexadecimal commit prefixes, real hunk headers, compound comment identity (#12).
- Transactional immutable snapshots with unchanged-source reuse, rollback and latest uniqueness (#13).
- Public PR REST collection, PAT support, pagination, retries and incomplete-source rejection (#14).
- Normalized Anthropic/chat-completions outcomes and complete request-text budgeting (#15).
- Generation, one repair, provenance, atomic claims/evidence and ingest/summarize/show CLI (#16).
- Reproducible offline/persisted fixture, reviewed sample, walkthrough and regression set (#17).

## Evidence recorded on 2026-09-07

- Locked dependencies installed with `uv sync --extra dev --frozen` in an isolated temporary environment.
- Ruff lint/format and strict pyright checked; full suite passed with PostgreSQL 16.
  The suite currently contains 94 passing tests, including database and CLI integration.
- Public `shashvatsinha/altiscope#2` collected through the actual CLI, saved as snapshot 1.
  A repeated collection reused the same snapshot ID/version.
- Credential-free `demo` reproduces the checked-in sample; `demo --persist` and `show`
  exercised the real source/account/evidence storage and renderer.
- Provider HTTP mocks exercise native success, invalid JSON/schema, refusal and truncation
  with available usage and request metadata. No paid model calls were made.
- Sample claims were reviewed against fixture patches, comments and omissions by the
  implementing agent. See the [review record](../../examples/m1/README.md).

## Release gates still open

- Owner/human review of the sample, implementation and proposed ADR-0009.
- Opt-in live model generation and output inspection through OpenRouter when
  `OPENROUTER_API_KEY` is configured.
  Live collection is verified; live model output quality is not.
- Repository PR review/CI requirements, milestone integration, release notes approval and tag.

Keep #4 open until these gates are satisfied. Do not present the fixture as model-produced
or call pointer validation semantic verification. The [walkthrough](../M1-WALKTHROUGH.md)
records exact commands, payload retention behavior and limitations.
