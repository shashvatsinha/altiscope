# PR review demonstration

[Read the sample review](sample.txt).

This synthetic fixture uses a hand-authored response, not a live model or real
acme/widgets PR. Run `uv run altiscope demo` without credentials, or
`uv run altiscope demo --persist` to exercise Postgres storage too.

The review describes the added lock and the limited test. The application attaches
the source PR link and discloses the excluded lockfile. No claim citations or
claim-level assessments are required. This demonstrates the workflow, not model quality.
