SYNTHETIC FIXTURE DEMO — recorded response, no live model call.
PR acme/widgets#42
https://github.com/acme/widgets/pull/42
Status: published; interpretation unverified

run() acquires a module-level lock around the working-directory lookup. A test for run() was added, but it only checks for a truthy result; it does not exercise concurrent callers.
Input omissions: 1 files excluded from model input.
